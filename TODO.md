# TODO

- [ ] Adjust spacing in Skills section: reduce top space inside/around `.skills-section` and reduce space between `.section-title` and the skills container (skills-wrapper / technical/professional areas).
- [ ] Adjust spacing around Services section: reduce space between `.section-title` and `.section-tagline`, and reduce the gap between the Services text and the skills container (services cards grid).
- [ ] Verify there are no conflicting duplicate `.skills-section`, `.services-section`, `.section-title` rules in other CSS files (especially `main.css` vs `skills.css` / `service.css`).
- [ ] Update CSS files and re-check layout consistency.

